---
- name: Configre backupserver
  hosts: backupserver
  become: true
  vars_files:
    - ../secrets.yml
  vars:
    tailscale_node_hostname: backupserver
  roles:
    - scheduled_update
    - scheduled_reboot
    - tailscale_node
