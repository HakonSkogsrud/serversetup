---
all:
  hosts:
    services:
      ansible_host: "{{ services.ip }}"
      ansible_user: "{{ services.user }}"
    immich:
      ansible_host: "{{ immich.ip }}"
      ansible_user: "{{ immich.user }}"
    mail:
      ansible_host: "{{ mail.ip }}"
      ansible_user: "{{ mail.user }}"
    proxmox:
      ansible_host: "{{ proxmox.ip }}"
      ansible_user: "{{ proxmox.user }}"