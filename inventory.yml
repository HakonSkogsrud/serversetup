---
all:
  hosts:
    services:
      ansible_host: "{{ services.ip }}"
      ansible_user: haaksk
    immich:
      ansible_host: "{{ server_ips.immich }}"
      ansible_user: haaksk
    mail:
      ansible_host: "{{ server_ips.mail }}"
      ansible_user: haaksk
    proxmox:
      ansible_host: "{{ server_ips.proxmox }}"
      ansible_user: root