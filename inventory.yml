---
all:
  hosts:
    services:
      ansible_host: "{{ vms.services.ip }}"
      ansible_user: "{{ vms.services.user }}"
    immich:
      ansible_host: "{{ vms.immich.ip }}"
      ansible_user: "{{ vms.immich.user }}"
    mail:
      ansible_host: "{{ vms.mail.ip }}"
      ansible_user: "{{ vms.mail.user }}"
    proxmox:
      ansible_host: "{{ vms.proxmox.ip }}"
      ansible_user: "{{ vms.proxmox.user }}"