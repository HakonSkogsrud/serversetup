---
- name: Create Promtail directories
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
    mode: "0755"
    owner: root
    group: root
  loop:
    - "{{ promtail_config_dir }}"
    - /var/lib/promtail
  become: true

- name: fetch latest version of promtail from github api
  ansible.builtin.uri:
    url: https://api.github.com/repos/grafana/loki/releases/latest
    return_content: yes
  register: github_api_output

- name: set version variable
  ansible.builtin.set_fact:
    latest_tag: "{{ github_api_output.json.tag_name }}"

- name: Download Promtail binary
  ansible.builtin.get_url:
    url: "https://github.com/grafana/loki/releases/download/{{ latest_tag }}/promtail-linux-amd64.zip"
    dest: "/tmp/promtail-{{ latest_tag }}.zip"
    mode: "0644"
  become: true

- name: Install unzip if not present
  ansible.builtin.package:
    name: unzip
    state: present
  become: true

- name: Extract Promtail binary to /tmp
  ansible.builtin.unarchive:
    src: "/tmp/promtail-{{ latest_tag }}.zip"
    dest: "/tmp"                # Extracting to /tmp is safer and always exists
    remote_src: true
  become: true

- name: Move and Rename Promtail binary to final destination
  ansible.builtin.copy:
    src: "/tmp/promtail-linux-amd64" 
    dest: "{{ promtail_install_dir }}/promtail"
    remote_src: true
    mode: "0755"
    owner: root
    group: root
  become: true

- name: Clean up temporary files
  ansible.builtin.file:
    path: "{{ item }}"
    state: absent
  loop:
    - "/tmp/promtail-{{ latest_tag }}.zip"
    - "/tmp/promtail-linux-amd64"
  become: true

- name: Deploy Promtail configuration
  ansible.builtin.template:
    src: promtail-config.yml.j2
    dest: "{{ promtail_config_dir }}/promtail-config.yml"
    owner: root
    group: root
    mode: "0644"
  become: true
  notify: Restart promtail

- name: Deploy Promtail systemd service
  ansible.builtin.template:
    src: promtail.service.j2
    dest: /etc/systemd/system/promtail.service
    owner: root
    group: root
    mode: "0644"
  become: true
  notify: Restart promtail

- name: Enable and start Promtail service
  ansible.builtin.systemd:
    name: promtail
    enabled: true
    state: started
    daemon_reload: true
  become: true

