---
- name: Run Tailscale setup for backupserver
  ansible.builtin.import_tasks: ubuntu.yml
  when: ansible_facts['os_family'] == 'Debian'

- name: Run Tailscale setup for my vms
  ansible.builtin.import_tasks: alma.yml
  when: ansible_facts['os_family'] == 'RedHat'

