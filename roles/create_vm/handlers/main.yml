---
- name: Reboot VM
  ansible.builtin.command:
    cmd: "qm reboot {{ create_vm_newid }}"
  listen: "reboot vm"

- name: Wait for VM to reboot
  ansible.builtin.pause:
    seconds: 30
  listen: "reboot vm"
