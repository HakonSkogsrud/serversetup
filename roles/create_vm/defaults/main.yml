---
# Hosts may override this to select a different source template. The primary
# keeps the existing AlmaLinux 9 template from group_vars unless a Proxmox
# host selects its AlmaLinux 10 template in host_vars.
create_vm_template_vmid: "{{ template.vmid }}"
