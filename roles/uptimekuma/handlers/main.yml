---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart uptimekuma
  community.docker.docker_compose_v2:
    project_src: "{{ uptimekuma_data_dir | dirname }}"
    state: present
  become: true
